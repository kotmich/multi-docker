sudo: required
services:
  - docker

before_install:
  - docker build -t kotmich/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true kotmich/react-test npm test

after_success:
  - docker build -t kotmich/multi-client ./client
  - docker build -t kotmich/multi-nginx ./nginx
  - docker build -t kotmich/multi-server ./server
  - docker build -t kotmich/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push kotmich/multi-client
  - docker push kotmich/multi-nginx
  - docker push kotmich/multi-server
  - docker push kotmich/multi-worker
  